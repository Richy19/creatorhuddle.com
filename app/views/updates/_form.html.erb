<%= simple_form_for update do |f| %>
  <%= f.input :content, as: :pagedown %>
  <%= f.input :updateable_id, as: :hidden %>
  <%= f.input :updateable_type, as: :hidden %>

  <div class="form-actions">
    <%= f.submit 'Save', class: 'btn btn-success' %>
  </div>
<% end %>
