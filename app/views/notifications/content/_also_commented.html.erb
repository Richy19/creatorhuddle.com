<%
  update = notification.target.commentable
  sender = notification.sender
  project = update.updateable
%>

<%= sender.decorate.name %> also commented on an <%= link_to 'update', "#{project_path(project)}\#update-#{update.id}" %> on <%= link_to project.name, project_path(project) %>
