<%
  update = notification.target
  sender = notification.sender
  project = update.updateable
%>

<%= sender.decorate.name %> posted an <%= link_to 'update', update_url(update) %> to <%= link_to project.name, project_url(project) %>
