<%
  update = notification.target.commentable
  sender = notification.sender
  project = update.updateable
%>

<%= sender.decorate.name %> also commented on an <%= link_to 'update', update_path(update) %> on <%= link_to project.name, project_url(project) %>
